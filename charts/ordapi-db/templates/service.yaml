apiVersion: v1
kind: Service
metadata:
  name: {{ include "ordapi-db.fullname" . }}
  labels:
    {{- include "ordapi-db.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 1433
      protocol: TCP      
  selector:
    {{- include "ordapi-db.selectorLabels" . | nindent 4 }}
